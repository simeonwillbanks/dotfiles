if has("gui_macvim")
  macmenu &File.New\ Window key=<nop>
  nmap <D-n> :NERDTreeToggle<CR> :NERDTreeMirror<CR>
endif

"don't show help ballons from any plugin
set noballooneval

"don't allow MacVim to show help ballons for netrm
let g:netrw_nobeval = 1
